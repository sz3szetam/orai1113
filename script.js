let obj = "";
function isEmpty(object)
{
    if(obj.length === 0)
    {
        console.log(true);
    }
    else
    {
        console.log(false);
    }
    
    
}
let emptyObj = isEmpty(obj);
